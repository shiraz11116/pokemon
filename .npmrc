# Railway deployment configuration for npm
# Skip Puppeteer Chromium download to avoid timeout
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
PUPPETEER_CACHE_DIR=/tmp/.puppeteer_cache

# Speed up npm install
progress=false
audit=false
fund=false